First Open a terminal and run these following commands to create the top-level folder it in VS Code
create project folder and open in VS Code
mkdir my-whiteboard
cd my-whiteboard
code .

Initialize git
Create backend folder and initialize Node app
Create frontend (Vite + React)
Frontend files structure & key file contents
Run both apps

try to improvements Add rooms join room IDs (socket.join) to isolate users using socket.io
try to add multiuser 
